# MVCreator Agent Rules

このファイル配下（このプロジェクト全体）での作業ルールです。

1. 各依頼の最終回答の末尾に、必ず完了時刻を1行で記載すること。
2. 完了時刻の書式は Markdown リンク形式とすること。
   ANSIカラーコードは使用しないこと。
   書式は以下とする。

   [完了時刻: YYYY-MM-DD HH:MM:SS (JST)](./_Worker/YYYY-MM-DD_HHMMSS.md)

   リンク先は、その作業で作成した `_Worker` 配下の作業記録 .md ファイルを指定すること。
3. このフォルダで作業を開始した時点で、本ルールを自動適用すること。
4. ユーザーへの回答は原則として日本語で行うこと。
5. 作業フォルダはこのファイルがあるフォルダがルートフォルダとして使用すること。
6. 作業が開始された場合、前日までの作業内容をプロジェクトルート下の「_WorkHistory」各.mdファイルを参照すること。
7. ユーザーが `作業終了` と明示した場合は、その日の作業内容をプロジェクトルート下の「_WorkHistory」へ `Update_YYYY-MM-DD-連番.md` 形式で書き出すこと。同名ファイルがある場合は末尾の連番をインクリメンタルしてください。
8. ユーザーが  `作業報告記載` と明示した場合は、当日のそれまでの作業内容をプロジェクトルート下の「_WorkHistory」へ `Update_YYYY-MM-DD-連番.md` 形式で書き出すこと。同名ファイルがある場合は末尾の連番をインクリメンタルしてください。
9.一つの作業指示が完了されたら、上記の日時で作業内容（依頼内容、修正内容）をフォルダ「_Worker」に（なければ作って）`YYYY-MM-DD_HHMMSS.md` 形式で作ってください。
   最終回答の末尾には、その作業記録ファイルへの Markdown リンクとして完了時刻を記載してください。

   例:
   [完了時刻: 2026-06-14 21:23:10 (JST)](./_Worker/2026-06-14_212310.md)